//export async function tab(name) {
  //const xpath = `//complex-tabs__label//*[contains(text(), '${name}')]`;
  //const element = await page.waitForXPath(xpath, { timeout: 10000 });
  //page.click('[id="mat-tab-label-0-1"]');
  //await page.waitForTimeout(2000);
//}
